## 1. Contract updates

- [x] 1.1 Update `Set-Cookie-Refresh` and `Set-Cookie-Refresh-Cleared` descriptions in `backend/openapi.yaml` to document `Domain` default/optional behavior.
- [x] 1.2 Mirror the same wording in `openspec/specs/openapi.yaml`.
- [x] 1.3 Extend auth/session specs with explicit `Domain` policy wording.

## 2. Verification

- [x] 2.1 Add/adjust contract tests that assert `Domain` policy language exists.
- [x] 2.2 Run focused tests for OpenAPI contract checks.
- [x] 2.3 Validate change with OpenSpec verify.
